describe("synth", () => {
  describe("constructor", () => {
    it("missing synthVoice", () => {
      expect(new synth()).toThrow();
    });
  });
  describe("init", () => {
    it("works", () => {});
  });
});
